import React from 'react';
import Headline from '../Headline/Headline';
import css from './CMSOverview.scss';
import CMSTiles from '../CMLTiles/CMSTiles';

const CMSOverview: React.FunctionComponent = () => {
  return (
    <React.Fragment>
      <Headline text={'CMSOverview'}></Headline>
    </React.Fragment>
  );
};
export default CMSOverview;
