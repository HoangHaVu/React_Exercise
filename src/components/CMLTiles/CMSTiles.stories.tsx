import { storiesOf } from '@storybook/react';
import React, { Fragment, ReactElement } from 'react';
import CMSTiles from './CMSTiles';

storiesOf('CMSTile', module).add('CMSTiles', () => <CMSTiles />);
